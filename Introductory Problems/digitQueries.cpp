#include<bits/stdc++.h>
using namespace std;

#define int long long

signed main() {
    int q;
    cin >> q;

    vector<int> queries(q);
    for(int i=0; i<q; i++) cin >> queries[i];

    

    // return 1;
}